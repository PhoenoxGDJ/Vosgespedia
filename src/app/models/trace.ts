export class Trace {
    trid: number;
    trace_name: string;

    constructor(
        trid: number,
        trace_name: string,
    ) {
        this.trid = trid;
        this.trace_name = trace_name;
    }
}
